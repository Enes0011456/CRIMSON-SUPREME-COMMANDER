/ / / / Gerekenler bunlardır bunları kurmanız lazım:

Python 3+

pip install scapy
pip install rich
pip install requests

Linux (Ubuntu önerilir) çünkü öbür sistemlerde test edilmedi

Sanal ağ / lab ortamı

Root (sudo) yetkisi

/ / / / Kurulum:

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

/ / / / Çalıştırma:

sudo python3 (dosya adı).py
//////////////ilk komut olmazsa bunu deneyin:
sudo -E venv/bin/python CRIMSON_SUPREME_COMMANDER.py
